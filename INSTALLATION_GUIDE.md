# 🎵 音量控制工具 - 完全安装和使用指南

## 🎯 一句话总结

**一个Windows桌面应用，可以为每个运行中的应用独立控制音量，支持自动监听和自动降音量功能。**

---

## ⚡ 5分钟快速开始

### 对于普通用户

1. **下载项目文件**
   - 如果已有项目文件夹，跳过此步
   - 否则从GitHub克隆或下载压缩包

2. **三种启动方式（选一种）**

   **方式A：直接运行（最简单）**
   ```
   双击 run.bat 文件
   ```
   自动安装依赖并启动应用

   **方式B：命令行运行**
   ```bash
   # Windows PowerShell 或 CMD
   python main.py
   ```

   **方式C：从EXE运行（推荐）**
   ```
   1. 运行 build.bat 构建EXE
   2. 双击 dist\VolumeControl.exe
   ```

3. **打开一个有声音的应用**
   - 在浏览器播放视频
   - 或打开音乐播放器

4. **调节音量**
   - 在应用列表中拖动滑块
   - 或点击"重置"或"🔇 静音"

完成！ 🎉

---

## 📦 安装步骤（详细版）

### 前置要求

**检查清单：**
- [ ] Windows 7 或更高版本
- [ ] Python 3.7+ (如果运行源代码)
- [ ] 管理员权限（推荐）

### 步骤1：获取项目文件

**选项A：如果已有文件夹**
```
项目已在: c:\Desktop\PYTHON\Volume_Control
```

**选项B：如果需要下载**
```bash
# 使用 git
git clone https://github.com/yourusername/volume-control.git
cd volume-control

# 或下载压缩包并解压
```

### 步骤2：检查Python环境

```bash
# 检查Python是否安装
python --version

# 需要 Python 3.7 或更高版本
# 如果没有，请从 https://www.python.org/downloads/ 下载安装
```

### 步骤3：安装依赖

**方式A：自动安装（推荐）**
```bash
# 直接运行批处理脚本
run.bat
```

**方式B：手动安装**
```bash
# 在项目文件夹中打开命令行
pip install -r requirements.txt
```

**方式C：使用虚拟环境（开发者推荐）**
```bash
# 创建虚拟环境
python -m venv venv

# 激活虚拟环境
# Windows:
venv\Scripts\activate

# 安装依赖
pip install -r requirements.txt
```

### 步骤4：验证安装

```bash
# 检查环境
python check_environment.py

# 输出应该显示所有✓标记
```

### 步骤5：运行应用

```bash
# 方式A：直接运行
python main.py

# 方式B：使用快速启动脚本
run.bat

# 方式C：以管理员身份运行（推荐）
# 右键点击 run.bat → 以管理员身份运行
```

---

## 🖥️ 打包成EXE（供他人使用）

### 快速打包

```bash
# 双击运行
build.bat

# 或手动运行
python build.py
```

### 手动打包

```bash
# 1. 安装PyInstaller
pip install PyInstaller

# 2. 构建EXE
pyinstaller --onefile --windowed --name=VolumeControl main.py

# 3. 生成的EXE在 dist/VolumeControl.exe
```

### 分发EXE

生成的 `dist/VolumeControl.exe` 可以：
- ✓ 直接发送给其他人
- ✓ 在任何Windows电脑上运行
- ✓ 不需要安装Python
- ✓ 不需要其他依赖

---

## 🎨 界面说明

```
┌─────────────────────────────────────────────────┐
│  🔊 音量控制工具                                │
├──────────────┬──────────────────────────────────┤
│ [🎵应用音量] [⚙️设置]                          │
├─────────────────────────────────────────────────┤
│                                                 │
│  应用名称      音量调节       音量%  按钮       │
│  ─────────────────────────────────────────────  │
│  Chrome      [████████░░░░] 60%  [重置][🔇]   │
│  Spotify     [██████░░░░░░] 40%  [重置][🔇]   │
│  Firefox     [███████░░░░░] 50%  [重置][🔇]   │
│                                                 │
└─────────────────────────────────────────────────┘
```

### 标签页说明

#### 🎵 应用音量 标签页
- **应用列表** - 显示所有正在播放声音的应用
- **音量滑块** - 拖动调节 0-100%
- **百分比显示** - 实时显示当前音量
- **重置按钮** - 恢复到默认音量
- **🔇 静音按钮** - 快速让应用无声

#### ⚙️ 设置 标签页
- **默认应用音量** - 新打开的应用使用这个音量
- **启用自动音量控制** - 勾选后新应用自动降到默认音量
- **提示信息** - 使用说明和快捷提示

---

## 🎯 使用场景

### 场景1：居家办公
```
配置：
- 默认音量: 30%
- 自动控制: 启用

效果：
- 工作时音乐不会太吵
- 突然打开新应用不会吓到你
- 集中力更好
```

### 场景2：娱乐休闲
```
配置：
- 默认音量: 80%
- 自动控制: 启用

效果：
- YouTube视频音量合适
- 看电影效果更好
- 各个应用音量均衡
```

### 场景3：会议中
```
配置：
- 默认音量: 0%
- 自动控制: 启用

效果：
- 防止突发噪音
- 应用打开时自动静音
- 会议不会被打扰
```

---

## 🔍 实用技巧

### 💡 技巧1：为不同场景保存配置
1. 在设置标签页调整音量
2. 手动编辑 `config.json` 保存预设
3. 需要时直接替换配置文件

### 💡 技巧2：快速创建快捷方式
```
1. 右键 run.bat → 创建快捷方式
2. 将快捷方式放在桌面
3. 右键快捷方式 → 属性
4. 在"高级"中勾选"以管理员身份运行"
5. 今后直接双击运行
```

### 💡 技巧3：开机自启
```
1. 将快捷方式复制到:
   C:\Users\用户名\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Startup
2. 下次开机会自动启动
```

### 💡 技巧4：性能优化
如果运行缓慢：
- 关闭其他后台程序
- 更新音频驱动
- 编辑 main_window.py，将监听间隔改为2000ms

### 💡 技巧5：精确控制
- 某些应用有内部音量控制，可以同时使用
- 系统音量 + 应用音量 = 最终音量
- 调节系统音量来微调总体效果

---

## 🐛 常见问题快速解决

| 问题 | 解决方案 |
|------|---------|
| 应用列表为空 | 打开浏览器播放视频或音乐 |
| 无法调节音量 | 右键以管理员身份运行 |
| 启动很慢 | 检查系统资源，关闭其他程序 |
| 依赖缺失错误 | 运行 `pip install -r requirements.txt` |
| UI显示异常 | 改为100%显示缩放，重启应用 |

详见 **TROUBLESHOOTING.md** 获取完整解决方案。

---

## 📁 项目文件说明

### 快速启动文件
- `run.bat` - 一键启动应用（自动安装依赖）
- `build.bat` - 一键构建EXE

### 主程序
- `main.py` - 应用入口
- `core/` - 核心功能模块
- `ui/` - 用户界面模块

### 工具和配置
- `requirements.txt` - Python依赖
- `check_environment.py` - 环境检查
- `setup.py` - pip安装配置

### 文档
- `README.md` - 完整文档
- `QUICK_START.md` - 快速开始
- `TROUBLESHOOTING.md` - 故障排除
- `PROJECT_STRUCTURE.md` - 项目结构

详见 **PROJECT_STRUCTURE.md** 了解更多。

---

## 🔧 开发者信息

### 修改代码后测试
```bash
# 1. 激活虚拟环境（如果使用）
venv\Scripts\activate

# 2. 安装最新依赖
pip install -r requirements.txt

# 3. 运行应用
python main.py

# 4. 检查是否有错误
# 如果有错误，查看控制台输出
```

### 添加新功能
1. 在相应的模块中添加代码
2. 在 `ui/` 中添加UI元素
3. 测试功能
4. 重新构建EXE

### 项目依赖
- `PyQt5` - 用户界面
- `pycaw` - 音量控制
- `psutil` - 进程管理
- `comtypes` - Windows COM接口

---

## 🚀 下一步

### 新用户
1. ✓ 安装并运行应用
2. ✓ 调整默认音量
3. ✓ 启用自动控制
4. ✓ 享受！

### 开发者
1. ✓ 克隆项目
2. ✓ 安装依赖
3. ✓ 查看源代码
4. ✓ 添加新功能
5. ✓ 构建和分发

### 想要贡献？
- 报告bug
- 建议功能
- 提交改进
- 翻译文档

---

## 📞 获取帮助

### 自助资源
1. 查看 **QUICK_START.md** - 快速开始指南
2. 查看 **TROUBLESHOOTING.md** - 常见问题解决
3. 查看 **README.md** - 完整文档
4. 运行 `python check_environment.py` - 诊断环境

### 需要支持
- 检查项目的GitHub Issues
- 查看源代码中的注释
- 尝试在虚拟环境中重新安装依赖

---

## 📝 许可证

MIT License - 自由使用和修改

---

## 🎉 准备好了吗？

```bash
# 立即开始！
python main.py

# 或
run.bat

# 或
双击 dist\VolumeControl.exe
```

**祝你使用愉快！** 🎵
