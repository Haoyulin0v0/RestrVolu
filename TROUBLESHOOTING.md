# 🔧 故障排除指南

## 常见问题和解决方案

### 1. 🚨 应用启动失败

#### 症状
- 双击EXE无反应
- 黑屏闪过后消失
- 弹出错误对话框

#### 解决方案

**步骤1：以管理员身份运行**
1. 右键点击 `VolumeControl.exe`
2. 选择 "以管理员身份运行"
3. 如果弹出UAC对话框，点击 "是"

**步骤2：检查.NET Framework**
- 打开控制面板 → 程序 → 启用或关闭Windows功能
- 确保 ".NET Framework" 已勾选

**步骤3：重新安装依赖（源代码运行）**
```bash
pip install --upgrade PyQt5 pycaw comtypes psutil
python main.py
```

---

### 2. 📋 应用列表为空

#### 症状
- 应用界面打开但没有显示任何应用
- 即使有音乐播放也不显示

#### 可能原因及解决方案

**原因A：没有应用正在播放声音**
- ✓ 打开浏览器并播放YouTube视频
- ✓ 打开音乐播放器（Spotify、网易云等）
- ✓ 打开任何音频应用

**原因B：系统声音被静音**
- ✓ 检查右下角音量图标
- ✓ 确保系统音量不是0%
- ✓ 不是 "🔇 静音" 状态

**原因C：权限不足**
- ✓ 关闭应用
- ✓ 右键 `VolumeControl.exe` → "以管理员身份运行"
- ✓ 重新打开应用

**原因D：某些应用的特殊音频处理**
- 如果特定应用不显示，该应用可能使用了特殊的音频输出
- 尝试重启应用或更新其驱动程序

---

### 3. 🔊 无法调节某个应用的音量

#### 症状
- 应用显示在列表中
- 但拖动滑块没有效果
- 点击按钮无反应

#### 解决方案

**步骤1：以管理员身份运行**
```
右键 → 以管理员身份运行 → 是
```

**步骤2：检查应用类型**
某些应用不支持独立音量控制：
- 系统提示音
- 某些游戏
- 旧版软件

**步骤3：重启应用**
1. 完全关闭该应用
2. 在音量控制工具中等待其从列表消失
3. 重新打开应用

**步骤4：更新驱动**
- 更新音频驱动程序
- 右键点击 → 设备管理器 → 声音、视频和游戏控制器
- 更新最新驱动

---

### 4. 🖱️ UI显示异常

#### 症状
- 按钮不显示或显示不完整
- 滑块无法使用
- 文字乱码或看不清

#### 解决方案

**解决方案A：重新安装PyQt5**
```bash
pip uninstall PyQt5
pip install PyQt5==5.15.9
```

**解决方案B：检查显示缩放**
1. 右键桌面 → 显示设置
2. 查看 "缩放与布局" 的百分比
3. 如果是125%或150%，尝试改为100%

**解决方案C：重新启动应用**
- 完全关闭应用
- 重新打开

---

### 5. 💾 配置文件找不到

#### 症状
- 设置没有保存
- 关闭后重新打开设置都重置了

#### 解决方案

**找到配置文件：**
1. 打开文件浏览器
2. 按 `Ctrl + L` 进入地址栏
3. 输入：`%USERPROFILE%\.volume_control`
4. 按 Enter

**或手动路径：**
- `C:\Users\你的用户名\.volume_control\config.json`

**重置配置：**
1. 关闭应用
2. 删除 `.volume_control` 文件夹
3. 重新打开应用

---

### 6. 🔴 出现"权限拒绝"错误

#### 症状
- 弹出错误信息："Permission Denied"
- 无法控制应用音量

#### 解决方案

**立即解决：**
1. 右键 `VolumeControl.exe`
2. 点击 "以管理员身份运行"
3. 如果弹出UAC，点击 "是"

**永久解决（创建快捷方式）：**
1. 右键 `VolumeControl.exe` → 创建快捷方式
2. 右键快捷方式 → 属性
3. 点击 "高级"
4. 勾选 "以管理员身份运行"
5. 应用 → 确定
6. 今后使用这个快捷方式运行

---

### 7. 🐢 应用运行很慢

#### 症状
- 启动很慢
- 响应迟缓
- 调节音量时卡顿

#### 解决方案

**方案A：减少监听频率**
- 编辑 `ui/main_window.py`
- 找到 `self.msleep(1000)` 改为 `self.msleep(2000)`
- 这会让监听间隔从1秒改为2秒

**方案B：检查系统资源**
- 打开任务管理器（Ctrl + Shift + Esc）
- 查看CPU和内存使用情况
- 关闭其他占用资源的程序

**方案C：重启应用和电脑**
- 先关闭音量控制工具
- 重启电脑
- 重新启动应用

---

### 8. 🎤 音量控制后没有听到差异

#### 症状
- 虽然滑块动了，但声音没变化
- 显示音量改变了但听不出来

#### 可能原因

1. **应用本身有音量控制**
   - 某些应用（如浏览器标签页）有单独的音量控制
   - 检查应用内部的音量设置

2. **系统音量很低**
   - 右下角的系统音量也很重要
   - 增加系统总音量

3. **声卡问题**
   - 检查音箱/耳机是否正常
   - 尝试重启音频设备

4. **音频驱动问题**
   - 更新声卡驱动
   - 卸载并重新安装驱动

---

### 9. 🎯 从源代码运行时出错

#### 症状
- 运行 `python main.py` 出现错误
- ImportError 或 ModuleNotFoundError

#### 解决方案

**检查Python版本：**
```bash
python --version
```
需要 Python 3.7 或更高版本

**重新安装依赖：**
```bash
# 清除旧版本
pip uninstall PyQt5 pycaw comtypes psutil -y

# 重新安装
pip install -r requirements.txt

# 运行应用
python main.py
```

**使用虚拟环境（推荐）：**
```bash
# 创建虚拟环境
python -m venv venv

# 激活虚拟环境
# Windows:
venv\Scripts\activate

# 安装依赖
pip install -r requirements.txt

# 运行
python main.py
```

---

### 10. 📦 打包成EXE时出错

#### 症状
- 运行 `build.bat` 或 `python build.py` 出错
- 生成的EXE无法运行

#### 解决方案

**安装PyInstaller：**
```bash
pip install PyInstaller
```

**清理之前的构建：**
```bash
# 删除这些文件夹
rmdir /s build dist
```

**重新构建：**
```bash
python build.py
```

**或手动构建：**
```bash
pyinstaller --onefile --windowed --name=VolumeControl main.py
```

---

## 🆘 仍然无法解决？

### 收集诊断信息

如果问题仍未解决，请提供以下信息：

1. **Windows版本**
   - 右键 "此电脑" → 属性
   - 记下 "Windows 10/11" 和内部版本号

2. **Python版本**
   ```bash
   python --version
   ```

3. **错误消息**
   - 完整的错误文本

4. **应用运行日志**
   - 运行时的完整控制台输出

### 获取帮助

- 查看 README.md 了解更多功能
- 查看 QUICK_START.md 了解基本使用
- 检查是否有新版本可用

---

## 💡 预防措施

为避免问题，建议：

1. ✓ 始终以管理员身份运行
2. ✓ 定期更新音频驱动
3. ✓ 保持Windows系统最新
4. ✓ 不要删除配置文件夹
5. ✓ 定期备份配置

---

**祝你使用愉快！** 🎉
